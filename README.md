# DriveBook

Простое сайт-приложение для записи учеников на занятия по вождению.

## Что умеет

- выбор даты и свободного времени;
- выбор автошколы перед записью, если в сервисе подключено несколько автошкол;
- личный кабинет ученика с регистрацией через Supabase Auth;
- блок ближайших занятий в кабинете ученика;
- запись ученика без повторного ввода имени, телефона и email;
- ввод комментария к записи в боковой панели после выбора времени;
- плашка успешной записи после отправки формы;
- выбор зарегистрированного инструктора или варианта "любой свободный инструктор";
- стартовый экран выбора роли: ученик или инструктор;
- регистрация инструктора через Supabase Auth с email, телефоном, логином и паролем от 8 символов;
- админ-панель для одобрения или блокировки инструкторов;
- автошколы в админ-панели и ключи доступа для регистрации инструкторов;
- регистрация инструктора только по активному ключу автошколы;
- вход инструктора по логину или email;
- проверка телефона, email, дублей логина/email/телефона и очевидно тестовых контактов;
- отдельные настройки расписания для каждого инструктора: рабочие дни, начало и конец дня, перерыв, длительность занятия, ограничение записи по времени;
- закрытие отдельных дат для выходных, отпуска или экзаменов;
- настройки каналов уведомлений и список ближайших напоминаний;
- недельный календарь занятий инструктора;
- быстрый обзор занятий инструктора на сегодня и ближайшие дни;
- статусы заявок: новая, подтверждена, завершена, отменена;
- быстрые действия инструктора: позвонить, написать email, подтвердить, завершить или отменить заявку;
- мини-аналитика по заявкам, загрузке недели и email-согласиям;
- кабинет инструктора с журналом, редактированием и удалением только своих заявок;
- ученики видят только одобренных администратором инструкторов;
- синхронизация заявок, учеников, инструкторов и расписаний с Supabase;
- локальная копия данных в браузере через `localStorage` для быстрого открытия;
- экспорт заявок в CSV.

## Как открыть

Откройте `index.html` в браузере или запустите локальный сервер:

```bash
python3 -m http.server 5173
```

После этого откройте общий вход:

`http://localhost:5173/index.html`

Админ-панель теперь открывается отдельно:

`http://localhost:5173/admin.html`

Чтобы войти как инструктор, сначала создайте кабинет через кнопку "Создать кабинет". После этого администратор должен одобрить заявку.

## Публикация сайта

Для первого теста можно использовать бесплатный адрес GitHub Pages без покупки домена:

`https://nikitasex123.github.io/drivebook/`

В репозитории уже добавлен workflow `.github/workflows/pages.yml`. Если сайт не появился автоматически, откройте GitHub:

1. **Settings -> Pages**.
2. В блоке **Build and deployment** выберите **Source: GitHub Actions**.
3. Запустите workflow **Deploy static site to GitHub Pages** во вкладке **Actions** или просто сделайте новый push в `main`.

После публикации добавьте адрес GitHub Pages в Supabase:

1. **Supabase -> Authentication -> URL Configuration**.
2. **Site URL**: `https://nikitasex123.github.io/drivebook/`.
3. **Redirect URLs**: добавьте `https://nikitasex123.github.io/drivebook/`, `https://nikitasex123.github.io/drivebook/index.html` и `https://nikitasex123.github.io/drivebook/admin.html`.

Это нужно, чтобы письма подтверждения регистрации ученика или инструктора возвращали пользователя на опубликованный сайт.

## Supabase backend

Проект умеет работать в двух режимах:

- без Supabase: данные хранятся в браузере через `localStorage`;
- с Supabase: данные синхронизируются с таблицами `students`, `instructors` и `bookings`, а новые кабинеты учеников и инструкторов создаются через Supabase Auth.

Чтобы включить Supabase:

1. В Supabase откройте **SQL Editor**.
2. Вставьте и выполните SQL из файла `supabase/schema.sql`.
3. Откройте **Project Settings -> Data API / API Keys**.
4. Скопируйте `Project URL` и `anon public key`.
5. Вставьте их в `supabase-config.js`:

```js
window.DRIVEBOOK_SUPABASE = {
  url: "https://your-project.supabase.co",
  anonKey: "your-anon-public-key",
};
```

`service_role key` нельзя вставлять во frontend-файлы и нельзя коммитить в GitHub. Он понадобится позже только для backend-функций и Telegram/email-уведомлений.

### Обновление действующей базы

Если Supabase уже был создан раньше, выполните миграцию `supabase/admin-approval-rls.sql`.

Перед запуском замените строку:

```sql
where email = 'YOUR_ADMIN_EMAIL_HERE'
```

на email аккаунта, который должен стать администратором. Этот аккаунт уже должен существовать в Supabase Auth.

Если админ-модерация уже включена, а нужно добавить только автошколы и ключи регистрации, выполните миграцию `supabase/schools-invite-keys.sql`.

Если автошколы уже добавлены, а нужно включить улучшенный UX записи и статусы заявок, выполните миграцию `supabase/student-ux-instructor-status.sql`.

## Важно

Новые пароли учеников и инструкторов больше не сохраняются в таблицы профилей; они проходят через Supabase Auth. Старые тестовые кабинеты инструкторов временно могут входить по прежнему паролю, чтобы не потерять доступ во время разработки.

После включения админ-модерации новые инструкторы получают статус `pending` и не появляются у учеников, пока администратор не нажмет "Одобрить".

После добавления автошкол инструктор должен ввести активный ключ автошколы при регистрации. Администратор создает автошколу, копирует ключ и отправляет его только нужным инструкторам.

Для продакшена следующим шагом нужно перенести критичные действия в backend/Supabase Edge Functions и настроить подтверждение телефона через SMS-провайдера.

## Напоминание про SaaS-версию

Следующий слой после ключей автошкол: разделить сервис по автошколам как полноценный SaaS. Для этого стоит добавить ссылки вида `?school=autoschool-a`, роли владельца автошколы, отдельную аналитику по каждой автошколе, тарифы/лимиты и backend-функции для безопасных действий.
